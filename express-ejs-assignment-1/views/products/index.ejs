<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
</head>
<body>
    <% for(let p of Products) { %>
        <li><%= p.title %></li>
        <a href="products/update/<%= p._id %>">Edit</a>
        <% } %>
</body>
</html>